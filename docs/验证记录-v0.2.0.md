# v0.2.0 验证记录

日期：2026-09-22。

## 交付

- 安装包：`artifacts/SmartStorage-0.2.0-debug.apk`。
- 包名：`com.smartstorage.app`；versionCode：2；versionName：0.2.0。
- 支持 Android 8.0（API 26）及以上，目标 API 35。
- 文件大小：19,129,417 字节。
- SHA-256：`e43eb79fc13d0402f70b234de0975580ff8b0e655b1bd884d28faef346beafbd`。
- `apksigner verify --verbose` 通过，APK Signature Scheme v2 签名有效。

## 最终验证

清理应用构建产物后完整执行 `:app:clean :core:test :app:testDebugUnitTest :app:lintDebug :app:assembleDebug`，结果 BUILD SUCCESSFUL。

| 测试范围 | 数量 | 失败 |
| --- | ---: | ---: |
| 核心库存规则 | 17 | 0 |
| 备份与 Excel | 7 | 0 |
| 分层与网格 | 8 | 0 |
| Room 数据约束 | 2 | 0 |
| Repository 持久化与恢复 | 4 | 0 |
| Compose 组件交互 | 3 | 0 |
| 合计 | 41 | 0 |

Android Lint：`No issues found.`

新增验证覆盖跨层移动的数量/价值/路径和筛选、缩减有库存层的拦截、恢复历史层 ID、子容器限制、禁止撤销库存到已移除层、网格边界与旋转、旧版本与分层备份往返、层数据重启持久化，以及选层、日期取消/清除、容器卡片点击。

## 交互实现调整

初始底部面板在主机触摸测试中未能提交选择，最终改用 Material 3 单选对话框，保留搜索与自定义输入，点击交互测试通过。构建期间出现过沙箱缓存权限及增量编译元数据问题；最终结果来自清理后的完整构建。

## 验证边界

Compose 与数据库测试运行在 Robolectric 主机环境，不代表真机验收。尚未在 Android 设备上验证本轮卡片视觉、长按拖拽手感、各屏幕尺寸/大字体、布局网格触摸与系统日期弹窗表现。AI 与云备份仍只保留接口。

实现和验收路径见 [空间与组件优化说明](空间与组件优化-v0.2.0.md)。
